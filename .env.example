# ==========================================
# DevOps & Agent Platform Configuration
# ==========================================

# JWT Secrets for API User sessions
JWT_SECRET=supersecretkey_devops_platform_2026

# AI LLM Provider Keys (Optional: Falls back to robust simulated mode if left empty)
GEMINI_API_KEY=
OPENAI_API_KEY=
# Local standalone port mappings (uncomment if running outside docker containers)
# DATABASE_URL=sqlite:///./devops.db
# REDIS_URL=redis://localhost:6379/0
